pnpm test:changed
status=$?
if [ $status -ne 0 ]; then
  echo "Warning: Some tests are failing. Please check your code."
fi
exit 0

npx lint-staged
staged_files=$(git diff --cached --name-only --diff-filter=d)
git add $staged_files