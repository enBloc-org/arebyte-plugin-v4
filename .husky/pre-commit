pnpm test:changed
status=$?
if [ $status -ne 0 ]; then
  echo "\033[1;31mBREAKING CHANGE - check your tests"
fi
exit 0

npx lint-staged
staged_files=$(git diff --cached --name-only --diff-filter=d)
git add $staged_files